package com.tuum.account.dto.enumeration;

public enum ErrorCode {
    INVALID_REQUEST,
    UNEXPECTED,
    ACCOUNT_NOT_FOUND,
    BALANCE_NOT_FOUND,
    CURRENCY_NOT_ALLOWED,
    INSUFFICIENT_FUNDS,
    INVALID_AMOUNT,
}
